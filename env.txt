# Environment variables for Images API
# this file should always have the same content

PORT=3000
DB_USER=imagesapi_user
DB_PASSWORD=imagesapi_user
DB_SERVER=localhost 
DB_DATABASE=BEDProject
DB_PORT=1433

# JWT secret key for authentication
JWT_SECRET=JWT_key